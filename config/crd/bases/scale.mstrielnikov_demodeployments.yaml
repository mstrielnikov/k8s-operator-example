---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: demodeployments.scale.mstrielnikov
spec:
  group: scale.mstrielnikov
  names:
    kind: DemoDeployment
    listKind: DemoDeploymentList
    plural: demodeployments
    singular: demodeployment
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: DemoDeployment is the Schema for the demodeployments API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DemoDeployment defines the desired state of DemoDeployment
            properties:
              deploymentName:
                description: Foo is an example field of DemoDeployment. Edit demodeployment_types.go
                  to remove/update
                type: string
              replicas:
                format: int32
                type: integer
            required:
            - deploymentName
            - replicas
            type: object
          status:
            description: DemoDeploymentStatus defines the observed state of DemoDeployment
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
